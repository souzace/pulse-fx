{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest",
    "migrate:create": "node-pg-migrate --tsconfig tsconfig.json -m src/infra/migrations create",
    "migrate:up": "node-pg-migrate -r dotenv/config --tsconfig tsconfig.json -m src/infra/migrations up",
    "migrate:down": "node-pg-migrate -r dotenv/config --tsconfig tsconfig.json -m src/infra/migrations down"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.9.3",
    "@types/pg": "^8.20.0",
    "dotenv": "^17.4.2",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "axios": "^1.18.0",
    "node-pg-migrate": "^8.0.4",
    "pg": "^8.21.0"
  }
}
